services:
  - type: web
    name: hica-frontend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist -l 10000
    envVars:
      - key: VITE_API_BASE_URL
        sync: false
